# 공고 원문(비정형 텍스트)에서 조건을 구조화 추출할 때 쓰는 LLM 설정.
# 실제 키는 .env 에만 두고, 이 파일에는 절대 값을 적지 않는다.

# 사용할 provider: anthropic | openai
LLM_PROVIDER=anthropic

# provider별 모델 (미지정 시 각 provider의 기본값)
# ANTHROPIC_MODEL=claude-opus-4-8
# OPENAI_MODEL=gpt-4o-mini

# API 키
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
